# Splines2.jl Documentation

```@meta
CurrentModule = Splines2
```

*Splines2.jl* is a Julia package providing functions for fitting splines and using them in regression models via the `StatsModels.@formula` interface.

```@contents
Pages = [
        "api.md",
]
Depth = 1
```
